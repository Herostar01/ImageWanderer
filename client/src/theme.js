import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#1760a5",
            light: "skybllue"
        },
        secondary: {
            main: "#15c630",
        },
        otherColor: {
            main: "#999"
        }
    }
});

export default theme;  